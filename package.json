{
  "name": "@surikat/updf",
  "version": "0.4.13",
  "description": "Hopefully tiny and modular library to generate PDF's in JS",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "mocha --compilers js:babel-register --recursive test",
    "test.watch": "mocha --watch --color --compilers js:babel-register --recursive test",
    "compile": "rimraf dist && node scripts/build.mjs",
    "compile.watch": "rimraf dist && node scripts/dev.mjs",
    "prepare": "npm run compile"
  },
  "devDependencies": {
    "@es-exec/esbuild-plugin-start": "^0.0.5",
    "esbuild": "^0.17.19",
    "mocha": "^2.4.5",
    "rimraf": "^5.0.0",
    "should": "^8.3.0"
  },
  "keywords": [
    "PDF",
    "JS"
  ],
  "author": "Surikat AB",
  "license": "MIT"
}
